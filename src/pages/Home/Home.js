import React from 'react';

import Counter from '../../components/Counter';

const Home = () => (
  <div>
    <h1 className="text-center">Home Counter</h1>
    <Counter />
  </div>
);

export default Home;
